setono_sylius_redirect:
    form:
        redirect:
            destination:
                not_blank: La destination ne peut pas être vide
                regex: "La destination doit commencer par '/'"
            source:
                not_blank: La source ne peut pas être vide
                not_identical_to: La source et la destination ne peuvent pas être identiques
                regex: "La source doit commencer par '/'"
                unique_globally: 'Il y a déjà une redirection ayant pour source "{{ source }}". ID de la redirection : {{ conflictingId }}'
                unique_per_channel: 'Il y a déjà une redirection ayant pour source "{{ source }}" pour le canal "{{ channel }}". ID de la redirection : {{ conflictingId }}'
            source_must_not_equal_destination: La source doit être différente de la destination
            target_result_in_infinite_loop: 'Ceci entraine une boucle infinie.'
            only_404:
                required_when_non_404_redirects_disabled: "« Rediriger uniquement si la page source est introuvable » doit être activé, car les redirections autres que 404 sont désactivées dans la configuration du plugin."
